'use client';
import { useEffect, useState } from 'react';
import { userStore, gameState } from '../context/Context';
export default function Timer() {
    // const [time, setTime] = useState(0);
    // const { player1Ready, player2Ready, game } = gameState((state) => state);

    // useEffect(() => {
    //     if (player1Ready && player2Ready && !game) {
    //         interval = setInterval(() => {
    //             setTime(10 - 1)
    //         }, 1000)
    //     }
    // },[player1Ready, player2Ready, game])

    // return <div>timer: {time}</div>;
}
